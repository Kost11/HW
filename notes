Text # Якийсь текст
variable  # Якась перемінна
X # Щось
print(X) # Виводить інформацію в дужках
#Text # Коментарі увесь рядок після "#" буде ігноруватись компютерем
"Text", 'Text' # Строка в неї можнанаписати якийсь текст бо звичайний не застосовується
variable = X # Перемінна у ній зберігається якась інформація яку туди запишуть, назва не може починатися з цифри не можуть використовуватися пробіли і усе окрім цифр, літер та "_". Використовуй замість пробілив snake_case, CamelCase і kebab-case
input(X) # 
X = X # Прирівнювання    X = 10 X прирівнювнюється до десяти, X буде десять
X == X # Дорівнює    10 == 10 десять дорівнює десяти
X != X # Не дорівнює    10 != 5 десять не дорівнює п'яти
X - X # Віднімання    3 - 2 дає результат 1    5 - 5 дає результат 0
X + X # Додавання    3 + 3 дає результат 6    4 + 5 дає результат 9
X * X # Множення    2 * 2 дає результат 4    6 * 3 дає результат 18
X ** X # Зведення в ступінь    2 ** 6 дає результат 64    5 ** 2 дає результат 25
X / X # Ділення    2 / 2 дає результат 1    5 / 2 дає результат 2.5    6 / 3 дає результат 2
X // X # Ділення націло    5 // 2 дає результат 2    26 // 8 дає результат 3
X % X # Залишок від ділення    5 % 2 дає результат 1    30 % 3 дає результат 0
X < X # Менше    X менше X
X <= X # Менше або дорівнює    X менше або дорівнює X
X > X # Більше    X більше X
X => X # Більше або дорівнює    X більше або дорівнює X
None # Нічого
f"Text, {variable}" # Можна замість    "Text" variable variable "Text"    писати    fText, {variable}, {variable}, text
True # Правда також використовується як вімкнути
False # Не правда також використовується як вимкнути
if # 
elif # 
else # 
int() # 
float() # 
str() # 
